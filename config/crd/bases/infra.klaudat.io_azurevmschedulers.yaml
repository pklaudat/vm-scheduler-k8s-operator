---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: azurevmschedulers.infra.klaudat.io
spec:
  group: infra.klaudat.io
  names:
    kind: AzureVmScheduler
    listKind: AzureVmSchedulerList
    plural: azurevmschedulers
    singular: azurevmscheduler
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureVmScheduler is the Schema for the azurevmschedulers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureVmSchedulerSpec defines the desired state of AzureVmScheduler.
            properties:
              resourceGroup:
                minLength: 1
                type: string
              shutdownAt:
                pattern: ^([01][0-9]|2[0-3]):([0-5][0-9])$
                type: string
              startUpAt:
                pattern: ^([01][0-9]|2[0-3]):([0-5][0-9])$
                type: string
              subscriptionId:
                minLength: 1
                type: string
              timezone:
                type: string
              vmNames:
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - resourceGroup
            - shutdownAt
            - startUpAt
            - subscriptionId
            - vmNames
            type: object
          status:
            description: AzureVmSchedulerStatus defines the observed state of AzureVmScheduler.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
